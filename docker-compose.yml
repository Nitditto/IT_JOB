services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.Frontend
    ports:
      - 5173:5173
    # depends_on:
    #   - backend
  backend:
    build:
      context: .
      dockerfile: Dockerfile.Backend
    ports:
      - 8080:8080
    environment:
      - BACKEND_URL=http://localhost:5173